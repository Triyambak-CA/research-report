# macOS
.DS_Store

# Editor / tooling
.vscode/
.claude/

# Local memory buffers (personal, not for version control)
.remember/

# Temp / scratch
*.tmp
*~

# Research source documents (annual reports, DRHP, transcripts) - kept in-folder, not versioned
source-docs

# ht-ml.app publish sidecars - contain secret update_key, must stay local
*.ht-ml.json
